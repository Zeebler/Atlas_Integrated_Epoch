AtlasIntegratedEpoch = AtlasIntegratedEpoch or {}
AtlasIntegratedEpoch.L = AtlasIntegratedEpoch.L or {}

-- English strings are seeded in Utils.lua so the addon has safe defaults even
-- if a locale file is missing. Future locale files can override keys here.

